spring:
  application:
    name: first-service
  threads:
    virtual:
      enabled: true
logging:
  level:
    academy.devdojo: debug

server:
  servlet:
    context-path: /
  error:
    include-stacktrace: ON_PARAM
  tomcat:
    threads:
      max: 20